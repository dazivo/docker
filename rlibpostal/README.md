# Lambda service : Structured address parsing

This docker image was created as a test to verify the usefullness of 
the libpostal library https://github.com/openvenues/libpostal wrapped as 
as R API. 

The docker container serves as a lambda service that one can query against.

Oberservations : 
    - Slow performance
    - Results are acceptable, but not perfect. 
